import styled from "styled-components";

const Banner = () => {
  return (
    <Wrapper>
      Banner
    </Wrapper>
  )
};

const Wrapper = styled.div`
  min-height: 100vh;
  
`;

export default Banner;