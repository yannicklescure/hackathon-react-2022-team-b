import styled from 'styled-components';

const Homepage = () => {
    return (
        <Wrapper>
            <h1>Form Page</h1>
        </Wrapper>
    );
}

const Wrapper = styled.div``;

export default Homepage;
